split:
  seed: 42
  train_ratio: 0.7
  val_ratio: 0.15

features:
  use_spread: true

model:
  type: random_forest
  params:
    n_estimators: 200
    max_depth: null
    min_samples_split: 2
    min_samples_leaf: 1

reports:
  output_dir: /home/noamhev/git/HAKATON-FOF/model_coordinate/reports
